# === Docker ===
.docker/
*.dockerignore
# Se você costuma salvar as imagens como arquivo tar localmente
*.tar

# === Docker Compose & Ambientes ===
# Ignora arquivos de variáveis de ambiente que contêm senhas e chaves secretas
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# === Volumes de Dados (Muito Importante!) ===
# Se você mapeia o volume do banco de dados (MySQL, Postgres, etc.) para uma pasta local,
# ignore essa pasta para não subir gigabytes de dados de teste para o GitHub.
# Ajuste o nome da pasta de acordo com o seu docker-compose.yml (ex: pgdata/, mysql_data/)
data/
db_data/
.database/

# === Logs ===
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*